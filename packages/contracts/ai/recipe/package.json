{
  "name": "@kitchenflow/recipe-ai-contracts",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "description": "Deterministic validators and fixtures for recipe AI artifact contracts.",
  "scripts": {
    "test": "node ./test.mjs",
    "validate": "node ./validate.mjs",
    "check:drift": "node ./check-drift.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  }
}
