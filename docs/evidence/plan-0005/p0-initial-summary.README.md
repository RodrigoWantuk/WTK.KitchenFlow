# Superseded P0 summary (historical)

`p0-initial-summary.json` is an early PLAN-0005 P0 summary that still records CSS zoom (`approx-200pct-css-zoom-2`), a Compose-only `containers.count: 2`, and a future `nextAutomatedBatch` that has since been executed.

It is retained for audit only and is **superseded** by:

- `p0-round1-summary.json` (regenerated during current P0 runs)
- evidence-generation head `4d07afa066fd7994bbc36473242ee7a2d764ea70`
- CI artifacts `plan-0005-p0-evidence-<prHeadSha>`

Do not treat this file as the current P0 result.
