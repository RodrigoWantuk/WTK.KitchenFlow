# Browser smoke summary

Chromium smoke on the pinned SUT passed all 28 recorded scenarios at 360px, 768px, and 1280px. Coverage included keyboard-only navigation, reduced motion, en/pt-BR/es locale checks, profile-route scoping, dirty shell navigation, dirty logout Stay and Discard through pointer and keyboard, browser history behavior, post-save reload warning, and session-refresh warning.

The smoke report is generated as CI-style transient output in the detached worktree and intentionally is not copied into versioned evidence because it contains no additional durable assessment truth beyond the command result and this summary.
