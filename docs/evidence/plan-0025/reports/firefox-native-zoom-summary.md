# Firefox native zoom summary

Firefox 141.0 under Xvfb/Openbox passed native approximately 200% zoom (`Ctrl+Plus` / xdotool): Cook CTA and Pantry-item pointer activation both passed the element-from-point and DPR-corrected pointer check, and both keyboard Enter checks passed. CSS zoom was forbidden and not used.
